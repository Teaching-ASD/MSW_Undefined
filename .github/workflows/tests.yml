name: Automated test CI

on:
  push:
    branches: [docker] 

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    container: 97vix/moszekalmantest:1.0
    steps:
      - uses: actions/checkout@v2
      - name: Get googletest
        run: |
              cd /usr/src/gtest
              sudo cmake CMakeLists.txt
              sudo make
              sudo ln -st /usr/lib/ /usr/src/gtest/libgtest.a
              sudo ln -st /usr/lib/ /usr/src/gtest/libgtest_main.a
      - name: Run all tests
        run: make test
      - name: Upload file
        uses: actions/upload-artifact@v1
        with:
          name: styleAndPerformance
          path: styleAndPerformance.txt       
